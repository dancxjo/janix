[workspace]
resolver = "2"
members = [
    "crates/bran",
]

[workspace.package]
edition = "2021"
license = "MIT OR Apache-2.0"

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
codegen-units = 1
lto = true
