[package]
name = "bloom_app"
version = "0.1.0"
edition = "2021"

[dependencies]
thing_std = { path = "../../crates/thing_std" }
bloom = { path = "../../crates/bloom" }

[package.metadata.cargo-xbuild]
memcpy = false
