[package]
name = "clock"
version = "0.1.0"
edition = "2021"

[dependencies]
thing_std = { path = "../../crates/thing_std" }
models = { path = "../../crates/models" }
thing_codec = { path = "../../crates/thing_codec" }
abi = { path = "../../crates/abi" }
postcard = { version = "1.0", default-features = false }

[package.metadata.cargo-xbuild]
memcpy = false
