[package]
name = "clock_demo"
version.workspace = true
edition.workspace = true

[lib]
name = "clock_demo"
path = "src/lib.rs"

[dependencies]
userland = { path = "../../userland" }
runtime = { path = "../../runtime" }

[dev-dependencies]
kernel = { path = "../../kernel" }
abi = { path = "../../abi" }
