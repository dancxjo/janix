[package]
name = "rootfs"
version.workspace = true
edition.workspace = true

[lib]
name = "rootfs"
path = "src/lib.rs"

[dependencies]
abi = { path = "../../abi" }
thing_models = { path = "../../thing_models" }
userland = { path = "../../userland" }
userland_std = { path = "../../userland_std" }
runtime = { path = "../../runtime" }
