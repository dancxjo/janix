[package]
name = "genie_circles"
version = "0.1.0"
edition = "2021"
description = "A fleeting contribution: four circles dancing at boot time"

# Standalone crate (not part of main workspace)
[workspace]

[lib]
crate-type = ["cdylib"]

[dependencies]

[profile.release]
opt-level = "s"
lto = true
panic = "abort"
