[package]
name = "bridge_hosted"
version = "0.2.0"
edition.workspace = true
license.workspace = true

[dependencies]
hw = { path = "../hw" }
kernel_core = { path = "../kernel_core" }
abi = { path = "../abi" }

[features]
default = ["std"]
std = []
