[package]
name = "graph"
version = "0.1.0"
edition = "2021"

[dependencies]
abi = { path = "../abi" }
spin = "0.9"

[dependencies.alloc]
version = "1.0"
package = "rustc-std-workspace-alloc"
optional = true

[features]
default = []
