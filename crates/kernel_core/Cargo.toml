[package]
name = "kernel_core"
version = "0.2.0"
edition.workspace = true
license.workspace = true

[dependencies]
hw = { path = "../hw" }
thing_models = { package = "models", path = "../models" }
abi = { path = "../abi" }
spin = "0.9.8"
heapless = "0.8.0"
