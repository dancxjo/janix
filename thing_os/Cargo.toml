[package]
name = "thing_os"
version.workspace = true
edition.workspace = true

[lib]

[dependencies]
abi = { path = "../abi" }
thing_models = { path = "../thing_models" }
runtime = { path = "../runtime" }
spin = "0.10"
