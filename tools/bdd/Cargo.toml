[package]
name = "bdd"
version = "0.1.0"
edition = "2024"

[dependencies]
cucumber = "0.21"
tokio = { version = "1", features = ["rt-multi-thread", "process", "io-util", "time", "sync"] }
async-trait = "0.1"
clap = { version = "4", features = ["derive"] }
futures = "0.3"
