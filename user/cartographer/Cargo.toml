[package]
name = "cartographer"
version = "0.1.0"
edition = "2024"

[dependencies]
thing_os = { path = "../../thing_os" }
abi = { path = "../../abi" }
runtime = { path = "../../runtime" }

[build-dependencies]
flate2 = "1.0"
reqwest = { version = "0.11", features = ["blocking"] }
