[package]
name = "clock_demo"
version.workspace = true
edition.workspace = true

[lib]
name = "clock_demo"
path = "src/lib.rs"

[dependencies]
runtime = { path = "../../runtime" }
thing_os = { path = "../../thing_os" }

[dev-dependencies]
kernel = { path = "../../kernel" }
abi = { path = "../../abi" }
