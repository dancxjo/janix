[package]
name = "thing_std"
version = "0.1.0"
edition = "2021"

[dependencies]
abi = { path = "../../../crates/abi" }
models = { path = "../../../crates/models" }
postcard = { workspace = true, features = ["alloc"] }
serde = { workspace = true, features = ["derive"] }

[features]
default = []
