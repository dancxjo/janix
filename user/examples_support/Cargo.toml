[package]
name = "examples_support"
version = "0.1.0"
edition = "2021"
build = "build.rs"

[dependencies]
thing_os = { path = "../../thing_os" }
abi = { path = "../../abi" }
log = "0.4"
runtime = { path = "../../runtime" }
