[package]
name = "geographer"
version = "0.1.0"
edition = "2021"
build = "build.rs"

[dependencies]
thing_os = { path = "../../thing_os" }
abi = { path = "../../abi" }
thing_macros = { path = "../../thing_macros" }
thing_models = { path = "../../thing_models" }

[build-dependencies]
