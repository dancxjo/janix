[package]
name = "input_logger_demo"
version.workspace = true
edition.workspace = true

[lib]
name = "input_logger_demo"
path = "src/lib.rs"

[dependencies]
runtime = { path = "../../runtime" }
thing_models = { path = "../../thing_models" }
thing_os = { path = "../../thing_os" }

[dev-dependencies]
kernel = { path = "../../kernel" }
abi = { path = "../../abi" }
thing_os = { path = "../../thing_os" }
