[package]
name = "mode_manager"
version = "0.1.0"
edition = "2021"

[dependencies]
thing_os = { path = "../../thing_os" }
abi = { path = "../../abi" }
thing_models = { path = "../../thing_models" }
runtime = { path = "../../runtime" }

[features]
default = []
