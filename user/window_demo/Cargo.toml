[package]
name = "window_demo"
version.workspace = true
edition.workspace = true

[lib]
name = "window_demo"
path = "src/lib.rs"

[dependencies]
runtime = { path = "../../runtime" }
thing_os = { path = "../../thing_os" }

[dev-dependencies]
kernel = { path = "../../kernel" }
abi = { path = "../../abi" }
thing_os = { path = "../../thing_os" }
