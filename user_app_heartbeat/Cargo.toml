[package]
name = "user_app_heartbeat"
version.workspace = true
edition.workspace = true

[lib]
name = "user_app_heartbeat"
path = "src/lib.rs"

[dependencies]
userland = { path = "../userland" }

[dev-dependencies]
kernel_core = { path = "../kernel_core" }
