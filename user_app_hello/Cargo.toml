[package]
name = "user_app_hello"
version.workspace = true
edition.workspace = true

[lib]
name = "user_app_hello"
path = "src/lib.rs"

[dependencies]
userland = { path = "../userland" }

[dev-dependencies]
kernel_core = { path = "../kernel_core" }
