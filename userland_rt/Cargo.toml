[package]
name = "userland_rt"
version.workspace = true
edition.workspace = true

[lib]

[dependencies]
abi = { path = "../abi" }
kernel_core = { path = "../kernel_core" }

[features]
host = []
kernel = []
