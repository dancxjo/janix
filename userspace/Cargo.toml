[workspace]
members = [
    "standard",
    "services/sprout",
    "drivers/rtc_cmos",
    "apps/clock",
    "apps/interrupting_cow",
]
resolver = "2"

[profile.dev]
panic = "abort"
lto = true
opt-level = 1

[profile.release]
panic = "abort"
lto = true
opt-level = 3
