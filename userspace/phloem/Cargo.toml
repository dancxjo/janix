[package]
name = "phloem"
version = "0.1.0"
edition = "2021"

[lib]
name = "phloem"
path = "src/lib.rs"

[dependencies]
stem = { path = "../../stem", default-features = false }
abi = { path = "../../abi" }
