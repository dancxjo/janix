[package]
name = "stamen"
version = "0.1.0"
edition = "2021"

[lib]
name = "stamen"
path = "src/lib.rs"

[[bin]]
name = "stamen"
path = "src/main.rs"

[dependencies]
stem = { path = "../../stem", features = ["rt"] }
abi = { path = "../../abi" }

[features]
default = []
